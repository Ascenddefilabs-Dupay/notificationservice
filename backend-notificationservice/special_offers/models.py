# special_offers/models.py
from django.db import models
from PushNotification1.models import NotificationSettings, EmailVerificationNotification  # Import from notifications app

# No need to redefine the models here. Just reuse the models from the notifications app.
